# 여기콕 설계 문서

시스템 아키텍처와 데이터 모델(ERD). GitHub에서 다이어그램이 바로 렌더링됩니다.

## 시스템 아키텍처

```mermaid
flowchart LR
  U["사용자 브라우저"] -->|HTTPS| ALB["ALB / 리버스 프록시"]
  ALB --> APP

  subgraph APP["Spring Boot 앱 (EC2)"]
    direction TB
    WEB["정적 프론트<br/>HTML · CSS · JS"]
    API["REST API<br/>map · member · admin · support"]
    BATCH["배치 스케줄러<br/>적재 · 결제 대사 · 정리"]
  end

  APP -->|"JPA · JdbcTemplate · 세션"| DB[("Oracle Autonomous DB")]

  API -->|"결제 · AI"| EXT
  BATCH -->|"데이터 수집"| EXT

  subgraph EXT["외부 연동"]
    SEOUL["서울 열린데이터광장"]
    FTC["공정위 · 공공데이터포털"]
    REB["한국부동산원 R-ONE"]
    KIPRIS["특허청 KIPRIS"]
    NEWS["네이버 뉴스"]
    TOSS["토스페이먼츠"]
    GEMINI["Google Gemini"]
  end
```

- **정적 프론트**: 빌드 툴 없는 순수 HTML/CSS/JS. `common.css` 토큰 시스템을 공유하고 화면별 CSS/JS는 폴더가 소유.
- **인증/세션**: Spring Security 세션 기반. 세션은 `SPRING_SESSION` 테이블에 영속화(재배포·다중 인스턴스에도 로그인 유지, 스티키 세션 불필요).
- **배치**: 관리자 수동 트리거 + 스케줄(결제 대사·휴면 전환·이력/사용량 정리). 실행 이력은 `BATCH_JOB_LOG`, 실패는 웹훅 알림.
- **프록시 전제**: 운영은 ALB 1홉 뒤. 클라이언트 IP는 신뢰 프록시가 찍은 XFF의 가장 오른쪽 값만 사용(허용목록·레이트리밋 스푸핑 방지).

## 데이터 모델 (ERD)

### 1) 상권 데이터: 스타 스키마

`TRDAR`(상권)·`DIM_QUARTER`(분기)·`INDUTY`(업종)를 차원으로, 팩트 테이블이 이를 참조합니다. 아래는 대표 팩트이며 **매출·점포·유동인구·상주인구·집객·상권변화·아파트·임대료 8종이 같은 구조**를 따릅니다.

```mermaid
erDiagram
  TRDAR {
    varchar TRDAR_CD PK
    varchar TRDAR_NM
    varchar SIGNGU_NM
  }
  DIM_QUARTER {
    varchar STDR_YYQU_CD PK
  }
  INDUTY {
    varchar INDUTY_CD PK
    varchar INDUTY_NM
  }
  SALES {
    varchar TRDAR_CD FK
    varchar STDR_YYQU_CD FK
    varchar INDUTY_CD FK
    number  THSMON_SELNG_AMT
  }
  STORE_STAT {
    varchar TRDAR_CD FK
    varchar STDR_YYQU_CD FK
    varchar INDUTY_CD FK
    number  STOR_CO
  }
  STREET_POP {
    varchar TRDAR_CD FK
    varchar STDR_YYQU_CD FK
    number  TOT_FLPOP_CO
  }

  TRDAR       ||--o{ SALES : "상권별 매출"
  DIM_QUARTER ||--o{ SALES : "분기"
  INDUTY      ||--o{ SALES : "업종"
  TRDAR       ||--o{ STORE_STAT : "점포 수"
  DIM_QUARTER ||--o{ STORE_STAT : "분기"
  INDUTY      ||--o{ STORE_STAT : "업종"
  TRDAR       ||--o{ STREET_POP : "유동인구"
  DIM_QUARTER ||--o{ STREET_POP : "분기"
```

### 2) 서비스 도메인: 회원 · 결제 · 관리자

```mermaid
erDiagram
  MEMBER {
    number  MEMBER_ID PK
    varchar LOGIN_ID
    varchar EMAIL
    varchar ROLE
    varchar STATUS
    timestamp PLAN_UNTIL
  }
  PAYMENT_ORDER {
    varchar ORDER_ID PK
    number  MEMBER_ID FK
    varchar STATUS
    number  AMOUNT
    number  VERSION
  }
  INQUIRY {
    number INQUIRY_ID PK
    number MEMBER_ID FK
    number ANSWERED_BY FK
    varchar STATUS
  }
  ADMIN_USER {
    number  ADMIN_ID PK
    varchar LOGIN_ID
    varchar ROLE
  }
  NOTICE {
    number NOTICE_ID PK
    number AUTHOR_ADMIN_ID FK
  }
  ADMIN_AUDIT_LOG {
    number LOG_ID PK
    number ADMIN_ID FK
    varchar ACTION
  }
  FAVORITE {
    number MEMBER_ID FK
    varchar TRDAR_CD FK
  }
  LLM_REPORT {
    number REPORT_ID PK
    number MEMBER_ID FK
    varchar TRDAR_CD FK
  }
  SEARCH_LOG {
    number LOG_ID PK
    number MEMBER_ID FK
  }

  MEMBER     ||--o{ PAYMENT_ORDER : "구독 결제"
  MEMBER     ||--o{ FAVORITE : "찜"
  MEMBER     ||--o{ SEARCH_LOG : "검색 기록"
  MEMBER     ||--o{ LLM_REPORT : "AI 리포트"
  MEMBER     ||--o{ INQUIRY : "1:1 문의"
  ADMIN_USER ||--o{ INQUIRY : "답변"
  ADMIN_USER ||--o{ NOTICE : "작성"
  ADMIN_USER ||--o{ ADMIN_AUDIT_LOG : "감사 기록"
```

전체 컬럼과 제약(CHECK·UNIQUE·FK)은 [`src/main/resources/db/schema.sql`](../src/main/resources/db/schema.sql)에 있습니다(도메인 30개 + 세션 2개 테이블).

## 결제 상태 흐름

```mermaid
stateDiagram-v2
  [*] --> PENDING: 주문 생성
  PENDING --> PAID: 승인(토스 확정)
  PENDING --> FAILED: 승인 실패 / 방치
  FAILED --> PAID: 대사 복구(토스 DONE 확인)
  PAID --> CANCELED: 환불
  CANCELED --> [*]
  note right of CANCELED
    종결 상태.
    승인으로 되돌아가지 않음
    (canTransitionToPaid 가드)
  end note
```
